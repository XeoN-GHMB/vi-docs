{
  "dependencies": {},
  "devDependencies": {
    "vitepress": "^1.0.1"
  },
  "scripts": {
    "dev": "vitepress dev --port=8100",
    "build": "vitepress build",
    "preview": "vitepress preview"
  }
}